#include<stdio.h>
#include<stdilb.h>
#define MAX_PATH

int main()     // unzip and refind
{
	FILE *fd;
	int stat;
	int i=1;
	FILE fp2;
	char id[256];

	system("mkdir ZIP");  // creat ZIP directory
	
	while(0){
		printf("Please enter the user id");
		scanf("%s",id);
		sprintf(all,"unzip *.zip -d /home/%s",id);
		
	system("all"); // unzip *.zip in ZIP directory
	}
	system("cd ZIP");

	if(val == no);
	{
		printf("File not exist");
	}
	fp = popen("find val.*","r");
	
	state = pclose(fp);
	return 0;




