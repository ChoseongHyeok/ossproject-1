#include<stdio.h>
#include<stdilb.h>

int main()
{
	FILE *fd;
	int stat;
	int i=1;

	system("mkdir ZIP");
	system("unzip *.zip -d /home/ZIP");
	system("cd ZIP");

	if(val == no);
	{
		printf("File not exist");
	}
	fp = popen("find val.*","r");
	
	state = pclose(fp);
	return 0;



