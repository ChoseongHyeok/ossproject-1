#include<stdio.h>
#define MAXLINE 256

int main()
{
	FILE *fp;
	int state;

	char buff[MAXLINE];
	fp = popen(buf,"r");
	
	if(fp==NULL)
	{
		while(fgets(output,sizeof(output),p)!=NULL);

		printf("%s",output);
	}
	printf("hi");
}

